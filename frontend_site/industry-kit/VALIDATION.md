# 產業套件驗證

- 確認 `styles.css` 載入 `industry-kit/components.css`。
- 確認所有頁面載入 `industry-kit/interactions.js`。
- 確認手機寬度沒有水平溢出，互動目標至少 44px。
- 確認 `prefers-reduced-motion` 開啟時內容仍完整可讀。
- 確認停用 JavaScript 後主要內容仍存在。
- 外部元件只有在 `THIRD_PARTY_NOTICES.json` 有記錄且授權相容時才可交付。
